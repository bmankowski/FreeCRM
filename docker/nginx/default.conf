server {
  listen 8080;
  server_name dev.itconnect.pl;

  root /var/www/html/public;
  index index.php;

  client_max_body_size 64m;

  # Installer lives outside public/ in this repo layout.
  location ^~ /install/ {
    alias /var/www/html/src/Modules/Install/;
    index Install.php;
    location ~ \.php$ {
      include fastcgi_params;
      fastcgi_param SCRIPT_FILENAME $request_filename;
      fastcgi_index Install.php;
      fastcgi_pass app:9000;
    }
  }

  location / {
    try_files $uri $uri/ /index.php?$query_string;
  }

  location ~ \.php$ {
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    fastcgi_param PATH_INFO $fastcgi_path_info;
    fastcgi_index index.php;
    fastcgi_pass app:9000;
  }

  location ~* \.(?:css|js|jpg|jpeg|gif|png|ico|svg|woff2?|ttf|eot)$ {
    expires 7d;
    access_log off;
    try_files $uri =404;
  }
}

server {
  listen 443 ssl;
  server_name dev.itconnect.pl;

  ssl_certificate     /etc/nginx/certs/dev.itconnect.pl.fullchain.pem;
  ssl_certificate_key /etc/nginx/certs/dev.itconnect.pl.privkey.pem;

  root /var/www/html/public;
  index index.php;

  client_max_body_size 64m;

  location ^~ /install/ {
    alias /var/www/html/src/Modules/Install/;
    index Install.php;
    location ~ \.php$ {
      include fastcgi_params;
      fastcgi_param SCRIPT_FILENAME $request_filename;
      fastcgi_param HTTPS on;
      fastcgi_index Install.php;
      fastcgi_pass app:9000;
    }
  }

  location / {
    try_files $uri $uri/ /index.php?$query_string;
  }

  location ~ \.php$ {
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    fastcgi_param PATH_INFO $fastcgi_path_info;
    fastcgi_param HTTPS on;
    fastcgi_index index.php;
    fastcgi_pass app:9000;
  }

  location ~* \.(?:css|js|jpg|jpeg|gif|png|ico|svg|woff2?|ttf|eot)$ {
    expires 7d;
    access_log off;
    try_files $uri =404;
  }
}

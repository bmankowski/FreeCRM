---
description: "UiType and field-type system in FreeCRM — read when working on field rendering, getFieldDataType(), UiType classes, or QueryField conditions"
---

# UiType & field-type system

## Two distinct type resolutions — never conflate them

| Method | Owner | Purpose |
|---|---|---|
| `Field::getUiTypeName(): string` | `src/Modules/Base/Models/Field.php` | Semantic type for business logic: import, workflow, reports, query conditions. Uses `SEMANTIC_TYPE_MAP`. |
| `Field::getFieldDataType()` | same — alias for `getUiTypeName()` | Kept for backward compatibility. **No return-type annotation** (see pitfall below). |
| `WebserviceField::getFieldDataType()` | `src/Webservices/WebserviceField.php` | REST API type from `vtiger_ws_fieldtype` + `typeofdata` fallback. `V~O` → `'string'` is correct for the API but **wrong for UI**. Never use as a UI fallback. |
| `BaseUiType::getInstanceFromField()` | `src/Modules/Base/UiTypes/BaseUiType.php` | Picks the renderer class via `UITYPE_CLASS_MAP` (uitype int → class suffix). **Does not call `getFieldDataType()`** for known uitypes — fully independent of the webservice layer. |

### `field_kind` + `storage_type` (vtiger_field — refactor in progress)

Columns backfilled by `m260705_000001`; resolvers: `src/Field/FieldKind.php`, `src/Field/StorageType.php`. **Runtime still uses `uitype` / `typeofdata`** until Phase 1b/2b. Full status: `documentation/table_vtiger_fields_refactor.md`.

#### Which column replaces which (target state)

| New / authoritative column | Replaces (legacy) | Runtime method / map today | Phase |
|---|---|---|---|
| `mandatory` | `typeofdata` segment `~M` / `~O` | was `explode('~')[1]` | **Done** (Change 3/8) |
| `maximumlength` | `typeofdata` segment `LE~n` | was length in type string | **Done** (Change 2) |
| DB column DDL (`decimal(5,2)` etc.) | `typeofdata` `N~2~2` | unused in PHP | **Done** (Change 5) |
| `vtiger_field_constraints` | `typeofdata` `~OTH~…` | was in type string + hardcoded validators | **Done** (Change 4) |
| **`field_kind`** | **`uitype`** (int) + **`SEMANTIC_TYPE_MAP`** + webservice fallback for semantics | `getUiTypeName()` / `getFieldDataType()` | Phase 1b → then drop `uitype` |
| **`storage_type`** | **`typeofdata`** (whole column) | `getFieldType()` | Phase 2b → **DROP `typeofdata`** |

**Not replaced by `field_kind` / `storage_type`:** `presence`, `readonly`, `displaytype`, `maximumlength`, picklist tables, `vtiger_fieldmodulerel`, etc.

**Renderer note:** `BaseUiType::UITYPE_CLASS_MAP` still keys on **`uitype` int** today. Phase 1b wires semantics to `field_kind` first; mapping renderer class from `field_kind` (merge with `UITYPE_CLASS_MAP`) is a later step — until then LayoutEditor keeps dual-writing `uitype` + `field_kind`.

**Do not conflate `field_kind` with `storage_type`.** Example: uitype 300 `description` → `field_kind=text`, `storage_type=string` (`typeofdata=V`, column often TEXT).

| `storage_type` | `typeofdata` | Typical SQL | vs `string` / `text` |
|---|---|---|---|
| `string` | `V` | `varchar` | default short text |
| `text` | `M` | `TEXT` | memo token — rare (legacy refs with `M`); not the same as `field_kind=text` |
| `email` | `E` | usually `varchar` | email validation, mailto UI, filter ops — not a separate SQL type |
| `password` | `P` | usually `varchar` | masked input, hash on save (`user_password`); not long text |

`mandatory` is authoritative (column on `vtiger_field`); legacy `~M`/`~O` suffixes inside `typeofdata` were stripped in Change 8.

---

## UITYPE_CLASS_MAP — renderer selection (BaseUiType)

Located at `BaseUiType::UITYPE_CLASS_MAP`. Maps uitype integer directly to the UiType class name suffix used for rendering.

| uitype | Class suffix | Notes |
|---|---|---|
| 3, 4 | `RecordNumber` | |
| 8 | `TotalTime` | |
| 9 | `Percentage` | |
| 10, 26, 50–51, 57–59, 73, 75–76, 80–81, 101, 357 | `Reference` | All reference variants |
| 11 | `Phone` | |
| 13 | `Email` | |
| 15 | `Picklist` | Role-based single select |
| 16, 33 | `Multipicklist` | 33 = LayoutEditor `MultiSelectCombo`; DB values joined with ` \|##\|` |
| 17 | `Url` | |
| 19–21, 24, 300 | `Text` | Multi-line text variants |
| 28 | `DocumentsFileUpload` | |
| 30, 342 | `Recurrence` | |
| 32 | `Languages` | |
| 52 | `UserCreator` | `getDisplayValue()` / `getListViewDisplayValue()` resolve ID via `\App\Fields\Owner::getLabel()` — bez tego UI pokazuje surowe ID (np. `1` zamiast nazwy użytkownika) |
| 53 | `Owner` | |
| 54 | `Multiowner` | |
| 55 | `Salutation` / `StringType` | Disambiguated by field name: `salutationtype` → `Salutation`, anything else → `StringType` |
| 56, 156 | `Boolean` | |
| 61, 69 | `Image` | |
| 66 | `ReferenceProcess` | |
| 67 | `ReferenceLink` | |
| 68 | `ReferenceSubProcess` | |
| 71, 72 | `Currency` | |
| 85 | `StringType` | Skype field — no dedicated class |
| 98 | `UserRole` | |
| 117 | `CurrencyList` | |
| 120 | `SharedOwner` | |
| 301 | `Modules` | Single module picklist (Edit); multi-select in Custom View filters via `AdvanceFilter_Modules_Field_Js` |
| 359 | `ModulesMultipicklist` | Multi module picklist (` \|##\| ` storage); empty = global scope (EmailTemplates) |
| 302 | `Tree` | `getAllValue()` for advanced-search picklists in `Field::getFieldInfo()` |
| 303 | `Taxes` | |
| 304 | `InventoryLimit` | |
| 305 | `MultiReferenceValue` | |
| 306 | `MultiReference` | Manual multi-record reference; comma-separated CRM IDs in TEXT |
| 308 | `RangeTime` | |
| 309 | `CategoryMultipicklist` | Same picklist path as Tree — `getAllValue()` delegates to `Tree` |
| 310 | `CompanySelect` | |
| 311 | `MultiImage` | |
| 330 | `MultiAttachmentJson` | JSON multi-attachment (e.g. Candidates `cv_img_file`); not AJAX-editable |
| 358 | `MailSmtpSelect` | |
| *(unmapped)* | derived from `getFieldDataType()` | Fallback for custom/future uitypes |

Resolution order inside `getInstanceFromField()`:
1. `\App\Modules\{Module}\UiTypes\{Suffix}` — module-specific override
2. `\App\Modules\Base\UiTypes\{Suffix}` — base implementation
3. `BaseUiType` — generic fallback (renders `String.tpl`)

---

## SEMANTIC_TYPE_MAP — business logic type (Field)

Located at `Field::SEMANTIC_TYPE_MAP`. Maps uitype integer to the semantic string used by import, workflow, reports, and query conditions.

Key entries that differ from the naive uitype name:

| uitype | Semantic type | Why it matters |
|---|---|---|
| 3 | `autogenerated` | Not `recordNumber` — uitype 4 is `recordNumber` |
| 15 | `picklist` | Role-based single select |
| 16, 33 | `multipicklist` | 33 = LayoutEditor `MultiSelectCombo`; **must** match `UITYPE_CLASS_MAP` |
| 26 | `documentsFolder` | Not `reference` |
| 27 | `fileLocationType` | |
| 54 | `multiowner` | Not `owner` |
| 61 | `file` | Not `image` |
| 69 | `image` | Not `file` |
| 117 | `currencyList` | Not `currency` (71/72 are `currency`) |
| 359 | `modulesMultipicklist` | Multi CRM module picklist; values from `getModulesListValues()`, stored as ` \|##\| `. CustomView adv filter: `typeofdata` **`V`** (not `V~O`); ops `e/n/y/ny` — see `customview-filters.mdc`. **List search + QueryField:** see subsection below. |
| 305 | `multiReferenceValue` | Auto-aggregated from relations; read-only on Edit |
| 306 | `MultiReference` | Manual multi-pick via popup; comma-separated CRM IDs in TEXT; target module from `vtiger_fieldmodulerel` (implements `ReferenceListProvider`) |
| *(unmapped)* | delegated to `WebserviceField::getFieldDataType()` | Falls back to `vtiger_ws_fieldtype`; plain text fields get `'string'` via `typeofdata V~O` |

### MultiReference (306) — Edit UI

Looks like Multipicklist (`email_template_type`): Select2 `select2-selection--multiple` chips; click empty area → record popup (not a dropdown of all records).

| Piece | Location |
|---|---|
| Template | `layouts/basic/modules/Base/uitypes/MultiReference.tpl` |
| JS | `public/layouts/basic/modules/Base/resources/Edit.js` → `registerMultiReferenceFields()` (+ minify) |
| UiType | `src/Modules/Base/UiTypes/MultiReference.php` |
| QueryField | `src/QueryField/MultiReferenceField.php` |

- Hidden `input.sourceField[data-fieldtype="multiReference"][data-multiple="true"]` holds comma-separated IDs for save; `<select multiple class="chzn-select js-multi-reference-select">` has **no** `name` — options only for currently selected records.
- **Smarty:** write `multiple class="..."` on one line; a line break between `multiple` and `class` compiles to invalid `multipleclass=`.
- **Init order:** `app.changeSelectElementView()` runs on `document.ready` before `registerMultiReferenceFields()` — destroy existing Select2 and reinit with `tags: false`, `minimumResultsForSearch: Infinity`. Popup via `select2:opening` (preventDefault) + `openPopUp`; selection event `Vtiger.MultiReference.Selection`.
- **Remove chip (×):** mousedown on `.select2-selection__choice__remove` sets Select2 `unselecting` flag (same as `app.js`) so `select2:opening` / search-field focus do not reopen the popup.

### ModulesMultipicklist (359) — ListView search + QueryField

- **Storage:** ` |##| ` (same as multipicklist); empty = global scope (EmailTemplates).
- **QueryField:** `ModulesMultipicklistField` extends **`MultipicklistField`** (not `ModulesField`). `operatorE` / `operatorA` delegate to `operatorC` (“equals module X” = template assigned to that module).
- **List search:** `ListSearch.js` must map `modulesMultipicklist` → operator **`c`**. If omitted, default **`a`** + `ModulesField`-style `getValue()` array → `BaseField::operatorA()` → `strpos(array)` fatal.
- **fieldInfo:** `Field::getFieldInfo()` case `modulesMultipicklist` sets `searchOperator = 'c'`.
- **Distinct from uitype 301 `modules`:** single module pick; list search uses **`e`**.

### Image display URLs (uitype 69, 311)

Never use raw `storage/…` or `getImageDetails()['path']` in `<img src>`. Naming (no `getImagePath`):

| uitype | Web URL in templates/PHP |
|---|---|
| 69 (Users photo) | `$RECORD->getImageWebUrl()` |
| 311 (`MultiImage`) | `$FIELD_MODEL->getUITypeModel()->getImageWebUrl($attachmentId, $recordId)` |
| Other record images | `$IMAGE_INFO.url` from `getImageDetails()` (already `file.php?…`) |

Details: uitype 69 → `user-profile-photos.mdc`; uitype 311 → `documents-storage.mdc` → Image URL API.

---

## Record autonumbering (uitype 3/4 → `RecordNumber`)

Sequence config lives in **`vtiger_modentity_num`** keyed by `tabid` (one row per module). On save, `Base\UiTypes\RecordNumber::getDBValue()` → `\App\Fields\RecordNumber::incrementNumber($tabid)` reads `prefix`/`cur_id`/`postfix`, writes `prefix.cur_id.postfix` to the uitype-4 field, then bumps `cur_id`.

- **Missing-row gotcha:** a module can have a uitype-4 `number` field but **no** `vtiger_modentity_num` row → `incrementNumber()` returns `''`, so every new/duplicated record gets a **silently empty** number. Fix = insert the row (prefix + `cur_id` = max existing sequence + 1). Hit on `Candidates` (tabid 121, prefix `PRO`) and `ProjektyRekrutacyjne` (tabid 119, prefix `POT`); migrations `m260630_000002` / `_000004`.
- **Leading zeros:** `start_id` / `cur_id` are **`varchar(50)`** (migration `m260630_000003`; were `int`, which dropped zeros). `incrementNumber()` already preserves sequence *width* via `str_repeat('0', …)`, so store the padded value (e.g. `cur_id = 026965` → `PRO026965`, next `026966`). Fresh-install schema: `Base3.php` (`stringType(50)`).
- **Settings UI:** `index.php?module=Vtiger&parent=Settings&view=CustomRecordNumbering`. "Start Sequence" accepts leading zeros; backfill button "Update missing record sequence" → `CRMEntity::updateMissingSeqNumber()` (also width-preserving). Supported modules = `isentitytype=1 AND presence=0` with a uitype-4 field.

## UiType class files

Base implementations: `src/Modules/Base/UiTypes/<ClassName>.php`
Module overrides: `src/Modules/<Module>/UiTypes/<ClassName>.php`

**Critical rule**: module-specific UiType classes must extend their Base counterpart, not `BaseUiType` directly.

```php
// CORRECT — inherits getTemplateName() → 'uitypes/Picklist.tpl'
class Picklist extends \App\Modules\Base\UiTypes\Picklist { ... }

// WRONG — silently falls back to getTemplateName() → 'uitypes/String.tpl' for every field
class Picklist extends \App\Modules\Base\UiTypes\BaseUiType { ... }
```

Known module-specific overrides and their purpose:

| File | Extends | Reason to keep |
|---|---|---|
| `Calendar/UiTypes/Picklist.php` | `Base\UiTypes\Picklist` | `getListSearchTemplateName()` for `activitystatus` / `activitytype` |
| `Calendar/UiTypes/Datetime.php` | `Base\UiTypes\Datetime` | Calendar-specific datetime rendering |
| `Calendar/UiTypes/Time.php` | `Base\UiTypes\Time` | Companion `time_start` / `time_end` defaults |
| `Calendar/UiTypes/ReferenceSubProcess.php` | `BaseUiType` | Subprocess reference list for activities |
| `Events/UiTypes/Time.php` | `Calendar\UiTypes\Time` | Same as Calendar (no `Events\UiTypes\Datetime` — use `Base\UiTypes\Datetime`) |
| `Events/UiTypes/ReferenceSubProcess.php` | `Base` alias | |
| `Documents/UiTypes/Text.php` | `BaseUiType` | Documents-specific text rendering |
| `DocumentTemplates/UiTypes/Conditions.php` | `BaseUiType` | Custom field type, no Base counterpart |
| `DocumentTemplates/UiTypes/TemplateMembers.php` | `BaseUiType` | Custom field type |
| `DocumentTemplates/UiTypes/WatermarkImage.php` | `BaseUiType` | Custom field type |
| `HelpDesk/UiTypes/RangeTime.php` | `BaseUiType` | HelpDesk-specific range time |
| `Notification/UiTypes/StringType.php` | `BaseUiType` | |
| `Notification/UiTypes/Text.php` | `BaseUiType` | |
| `OSSTimeControl/UiTypes/Double.php` | `BaseUiType` | |
| `OSSTimeControl/UiTypes/Time.php` | `BaseUiType` | |
| `Reservations/UiTypes/Time.php` | `BaseUiType` | |
| `TemplateElements/UiTypes/StringType.php` | `BaseUiType` | |
| `Users/UiTypes/Boolean.php` | `BaseUiType` | |

---

## Template methods on UiType classes

| Method | Default (BaseUiType) | Used by |
|---|---|---|
| `getTemplateName()` | `uitypes/String.tpl` | EditView, QuickCreate |
| `getDetailViewTemplateName()` | `uitypes/StringDetailView.tpl` | DetailView |
| `getListSearchTemplateName()` | `uitypes/FieldSearchView.tpl` | ListView search row |

Templates live in `layouts/basic/modules/Base/uitypes/` (Base) or `layouts/basic/modules/<Module>/uitypes/` (override).
Compiled cache: `cache/templates_c/basic/` — clear with `rm -f cache/templates_c/basic/*.php` after template changes.

### ListView date range search — datetime needs the Date search template
The range date-picker on the list search row is wired in JS only to inputs with class `.dateField` + `data-calendar-type="range"`, which **only** `uitypes/DateFieldSearchView.tpl` emits (`ListSearch.js` → `registerDateListSearch()`, `mode: 'range'`). `Datetime` (uitype 70, e.g. `createdtime`) must override `getListSearchTemplateName()` to return `'uitypes/DateFieldSearchView.tpl'` — otherwise it falls back to the plain `FieldSearchView.tpl` text input (no calendar). `ListSearch.js` sends operator `bw` for both `date` and `datetime`. Picker has no OK button: value updates live on each day click; search fires on picker close (`DatePicker.onHide`) when `AUTO_REFRESH_RECORD_LIST_ON_SELECT_CHANGE` is on, or on **Enter** (document handler in `registerDateListSearchEnterKey()` — see `listview-javascript-playbook.mdc`).

### `operatorBw` with a single date = whole day
`bw` ("between") splits the value on `,`. A single date (e.g. from a URL/API `["createdtime","bw","2026-06-11"]`) leaves `$value[1]` null → empty upper bound → `BETWEEN` matches nothing. `DateField`/`DatetimeField::operatorBw()` fall back `$end` to `$value[0]` so one date covers that full day (`00:00:00`–`23:59:59` for datetime).

---

## Pitfalls

### Adding `: string` to `getFieldDataType()` causes a fatal error
`Field::getFieldDataType()` is overridden (without a return type) by 6 subclasses:
- `src/Modules/Calendar/Models/Field.php`
- `src/Modules/Users/Models/Field.php`
- `src/Modules/Settings/SMSNotifier/Models/Field.php`
- `src/Modules/Settings/MappedFields/Models/Field.php`
- `src/Modules/Settings/Leads/Models/Field.php`
- `src/Modules/Base/InventoryFields/Basic.php`

Use `getUiTypeName(): string` (which has a return type) when you need a typed call.

### Reference fields on DetailView — modal edit, not in-place
- **Gate:** `Field::isReferenceModalEditable()` (`datatype === reference`, `isEditable()`, `displaytype !== 10`). Separate from `isAjaxEditable()` (which still blocks uitype 10/51/59 for in-place).
- **Save contract:** same as other inline edits — `SaveAjax` with `field` + `value` (CRM id); `Reference::getDBValue()` normalizes empty → `0`; response `display_value` is HTML link.
- **Do not** render hidden `.edit` + `Reference.tpl` in DetailView cells — use modal (`ReferenceEditModal`) only.

### Reference autocomplete (uitype 10) — empty search results
Edit/QuickCreate `.autoComplete` → `BasicAjax` → `Module::searchRecord()` → `RecordSearch` on `u_yf_crmentity_search_label` with `userid LIKE '%,{userId},%'`. Missing `userid` on the label row = no hits. Fix via `PrivilegeUpdater` (see `cron.mdc` → `LBL_PRIVILEGES_UPDATER`). Popup search (`relatedPopup`) uses a different path and may still work.

### Multi-module reference (uitype 10) — dropdown order
Target modules from `vtiger_fieldmodulerel`, ordered `sequence ASC` in `Field::getReferenceList()`. **NULL `sequence` sorts before numbers** — e.g. HelpDesk `parent_id` had Vendors (NULL) before Accounts (1). Set explicit sequences (Accounts=1, Vendors=2); migration `m260702_000001_helpdesk_parent_id_accounts_before_vendors.php`.

### uitype 33 — renderer and semantic type must both be multipicklist
LayoutEditor creates `MultiSelectCombo` as uitype **33** (`Settings/LayoutEditor/Models/Module.php`). If `UITYPE_CLASS_MAP` or `SEMANTIC_TYPE_MAP` treats 33 as single `picklist`:
- DetailView shows raw ` \|##\|` delimiters instead of comma-separated labels
- DetailView inline edit finds hidden `name="field"` instead of `select[name="field[]"]` (`Detail.js` only appends `[]` when `data-type="multipicklist"`)
- `SaveAjax` skips `implode(' \|##\| ', …)` for array values

Both maps: **33 → `Multipicklist` / `multipicklist`**. uitype 33 is still **role-based** (`Field::isRoleBased()`). Example fields: `ProjektyRekrutacyjne.benefits`, `project_publishing_lang`.

### `WebserviceField` as a UI fallback silently breaks picklists
`typeofdata = V~O` maps to `'string'` in `WebserviceField`. If `SEMANTIC_TYPE_MAP` is missing an entry and falls through to `WebserviceField`, a picklist field gets type `'string'`, which causes `getInstanceFromField()` to use `StringType` → `String.tpl` → labels display as raw `PLL_*` keys instead of translated text.

### Stale per-request cache
`Field::$fieldDataType` is a per-instance property cache. It is set once and never invalidated within the same request. If a field model is reused with different context (e.g. same object, different module), the cached type from the first resolution persists. Do not reuse field model instances across modules.

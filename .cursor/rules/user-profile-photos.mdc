---
description: "User profile photos (avatars) — storage, upload, URLs, sidecars; read when touching Users photo or avatar display"
globs:
  - "src/Modules/Users/**"
  - "layouts/basic/modules/Users/**"
  - "layouts/basic/modules/Base/BodyLeft.tpl"
  - "layouts/basic/modules/Base/Comment*.tpl"
  - "layouts/basic/modules/Base/Recent*.tpl"
  - "layouts/basic/modules/Base/HistoryRelation.tpl"
  - "layouts/basic/modules/Base/RecentActivitiesTimeLine.tpl"
alwaysApply: false
---

# User profile photos

User avatars live in `storage/Users/…` and are **not** public — always serve via `file.php`.

## Data model

| Layer | Location |
|---|---|
| Metadata | `vtiger_users` — `photo_storage_path`, `photo_mime_type`, `photo_size_bytes`, `imagename` (display name) |
| Disk | `storage/Users/{year}/{month}/week{n}/{userId}_{time}_{name}.png` (legacy prod blobs may use `{attachmentsid}_` prefix) |
| Sidecar | `{photo_storage_path}.base64` — data-URI for mail/PDF (`getUserPhotoImgHtmlForGenerator()`) |

**User avatars are not stored in `s_yf_record_files`.** That table is for CRM entity record images (Contacts, HelpDesk, Products, …) and FKs to `vtiger_crmentity`. Users are not CRM entities — do not backfill `vtiger_crmentity` for photo uploads.

Migration: `m260710_000005_users_photo_storage_columns.php` (columns + move off `s_yf_record_files`).

## Upload (`Users::uploadAndSaveFile`)

1. Validate image; delete prior avatar (`unlinkUserAvatarFilesForUserId`).
2. Save to `initStorageFileDirectory('Users')` + `{userId}_{time}_{sanitizedName}`.
3. Normalize via ImageMagick → **300×300 PNG** (`Users\Models\Record::normalizeUploadedUserPhotoFile`, `USER_PHOTO_SIZE`, `USER_PHOTO_MIME = image/png`).
4. Update `vtiger_users` photo columns via `Record::saveUserPhotoMeta()`; write `.base64` sidecar.

Single master PNG — CSS scales (~50px list, ~40px timeline, ~70px header); no separate thumbnail files.

## URL API (never `storage/…` in `<img src>`)

| Use | Method |
|---|---|
| Web URL | `Users\Models\Record::getImageWebUrl()` → `file.php?module=Users&action=Image&record={id}` or default icon |
| Disk path (PHP only) | `getImageRelativePath()` |
| Read metadata | `Record::getUserPhotoMeta($userId)` |
| Resolve absolute | `RecordFile::resolveAbsolutePath($relativePath)` |
| Serve handler | `src/Modules/Users/Files/Image.php` |
| uitype 69 / comments / timeline | `$USER_MODEL->getImageWebUrl()` (ModComments delegates to commentor user) |
| PDF/HTML generator | `getUserPhotoImgHtmlForGenerator()` (sidecar data-URI) |

No `getImagePath()` — do not reintroduce.

## Templates

- Left panel: `BodyLeft.tpl` — `getImageRelativePath()` gates `userName--withPhoto`; `<img class="userNamePhoto">` uses `getImageWebUrl()`.
- CSS: `css.mdc` → Left panel header.

## Mail sidecars

Sidecar must hold **PNG/jpeg/gif** data URI (not WebP). Built by `writeUserPhotoBase64SidecarForRelativeImage()`; legacy WebP on disk is converted on write. PHPMailer embed rules: `mail-module.mdc` → Inline images.

## Diagnose & repair

```sql
SELECT id, user_name, imagename, photo_storage_path, photo_mime_type, photo_size_bytes
FROM vtiger_users
WHERE id = ?;
```

Missing `photo_storage_path` but file on disk under `storage/Users/`:

1. `UPDATE vtiger_users` with `photo_storage_path`, `imagename`, `photo_mime_type='image/png'`, `photo_size_bytes`.
2. Regenerate sidecar if needed: `Record::writeUserPhotoBase64SidecarForRelativeImage($relativePath, 'image/png')`.

Verify: `Record::getInstanceById($id, 'Users')->getImageRelativePath()` non-null; `RecordFile::resolveAbsolutePath()` readable.

**Deploy:** `storage/Users/` excluded from `pushsrc` — use `pushstorage` or targeted `rsync` (see `deployment.mdc`). `Image.php` serves default icon when file missing (no fatal).

General record-file storage (non-Users): `documents-storage.mdc`.
